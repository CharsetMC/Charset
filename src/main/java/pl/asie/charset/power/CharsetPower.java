package pl.asie.charset.power;

import pl.asie.charset.lib.annotation.CharsetModule;

@CharsetModule(
        name = "power",
        description = "Charset's own power system",
        isDevOnly = true
)
public class CharsetPower {
}
